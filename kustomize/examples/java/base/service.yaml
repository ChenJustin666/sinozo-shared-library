apiVersion: v1
kind: Service
metadata:
  name: SERVICE_NAME-svc
  labels:
    app: SERVICE_NAME
    app.kubernetes.io/name: SERVICE_NAME
spec:
  type: ClusterIP
  selector:
    app: SERVICE_NAME
  ports:
    - name: http
      port: 8080
      targetPort: http
