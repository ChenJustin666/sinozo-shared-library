# 示例文件，不要直接 apply。生产密钥必须在目标集群使用 kubeseal 加密。
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: SERVICE_NAME-runtime
  namespace: <project>-prod
spec:
  encryptedData: {}
  template:
    metadata:
      name: SERVICE_NAME-runtime
    type: Opaque
